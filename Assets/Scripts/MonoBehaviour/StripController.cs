using UnityEngine;

namespace MonoBehaviour
{
	public class StripController : UnityEngine.MonoBehaviour
	{
		private Transform _transform;

		// Use this for initialization
		void Start()
		{
			_transform = GetComponent<Transform>();
		}

		// Update is called once per frame
		void Update()
		{

		}
	}
}
